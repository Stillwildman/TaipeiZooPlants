<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.drawerlayout.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:openDrawer="start">

        <include
            android:id="@+id/includeAppBar"
            layout="@layout/include_app_bar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:paddingTop="@dimen/icon_common_size_xl"
            android:fitsSystemWindows="true"
            android:theme="@style/MyNavigationDrawerStyle"
            app:itemHorizontalPadding="@dimen/icon_common_size"
            app:itemBackground="?android:selectableItemBackground"
            app:itemTextColor="@color/md_white_1000"
            app:itemTextAppearance="?android:textAppearanceLarge"
            app:itemIconTint="@color/md_white_1000"
            app:menu="@menu/activity_main_drawer"/>

    </androidx.drawerlayout.widget.DrawerLayout>

</layout>


